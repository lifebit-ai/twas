
   
/*
 * -----------------------------------------------------------------
 *  twas test config file
 * -----------------------------------------------------------------
 * Defines bundled input files and everything required
 * to run a fast and simple test. Use as follows:
 * nextflow run main.nf -profile test_tsv

 */
 
 params {
    gwas_summary_statistics = "s3://lifebit-featured-datasets/pipelines/twas/testdata/Astle_et_al_2016_Red_blood_cell_count.gambit.tsv.gz"
    annotate_vcf = false
    report_dir = "/opt/bin"

    max_cpus = 2
}
